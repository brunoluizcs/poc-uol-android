<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:homeAsUpIndicator">@drawable/ic_action_arrow_back</item>
        <item name="homeAsUpIndicator">@drawable/ic_action_arrow_back</item>
    </style>

    <style name="AppTheme.Base" parent="AppTheme"/>


    
    <style name="AppTheme.LauncherScreen" parent="AppTheme">
        <item name="android:windowBackground">@drawable/background_launcher_screen</item>
    </style>

</resources>
